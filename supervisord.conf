;For a sample configuration file, refer to https://github.com/Supervisor/supervisor/blob/master/supervisor/skel/sample.conf
[inet_http_server]         ; inet (TCP) server disabled by default
port=*:9001                ; port number to bind Supervisor to (change 9001 if needed)
username=user              ; optional: username for HTTP authentication
password=pass              ; optional: password for HTTP authentication


[unix_http_server]
file=/tmp/supervisor.sock   ; (the path to the socket file)

[supervisord]
logfile=/home/ec2-user/app/supervisord.log ; (main log file;default $CWD/supervisord.log)
logfile_maxbytes=50MB        ; (max main logfile bytes b4 rotation;default 50MB)
logfile_backups=10           ; (num of main logfile rotation backups;default 10)
loglevel=info                ; (log level;default info; others: debug,warn,trace)
pidfile=/tmp/supervisord.pid ; (supervisord pidfile;default supervisord.pid)
nodaemon=false               ; (start in foreground if true;default false)
minfds=1024                  ; (min. avail startup file descriptors;default 1024)
minprocs=200                 ; (min. avail process descriptors;default 200)

[rpcinterface:supervisor]
supervisor.rpcinterface_factory = supervisor.rpcinterface:make_main_rpcinterface

[supervisorctl]
serverurl=unix:///tmp/supervisor.sock ; use a unix:// URL  for a unix socket

[program:fastapiApplication]
# command = nohup authbind gunicorn -w 4 -b 0.0.0.0:80 app:app --timeout 1800
# command = nohup uvicorn Server:app --host 0.0.0.0:80
command = uvicorn main:Server --host 0.0.0.0 --port 3002
# stdout_logfile = /var/log/fastapi-application-stdout.log
# stderr_logfile = /var/log/fastapi-application-stderr.log